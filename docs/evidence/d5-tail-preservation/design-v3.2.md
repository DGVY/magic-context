# D5 descent: predecessor-tail preservation — design v3 (2026-09-12)

Supersedes v2. Incorporates Thalamus pm_299f6cfb. Status: for Thalamus agreement → receipt schema + D calibration evidence → Athena gate → spec/impl. No production edits before all three.

## 0. Defect and ownership
Unchanged (v2 §0, §2). One ownership model: **seal on P, redeem on the successor**. P's in-flight fire is never a carrier; the epoch guard stays.

## 1. Vocabulary (final)
- **Source blocks:** native blocks Thalamus supplies for T (transport-normalized; recognized compaction-instruction additions excluded by provenance). Authoritative *material*.
- **Applied state (A):** MC's durable reduction/strip/frozen-unit state for T at seal: `red:*` units, skeletons, strip decisions, caveman units, tag identities, pending-drop rows, ledger rows. Authoritative *permissions*.
- **Projection (V):** `project(source, A)` — the provider-visible rendering MC would serve for T given A. Defined for EVERY manifest block, including blocks never yet served (see §4). V is computed at seal from source + A; it does not depend on MC having retained last-served bytes.
- **Archive:** V-tail bytes + manifest (§8) + A snapshot, durable, content-addressed (`archive_id = sha256(manifest ∥ V ∥ A)`).
- **Prepared-archive ctx_expand semantics (explicit, new, scoped):** on the successor, `ctx_expand(ordinal ∈ T)` resolves to the archive and returns **V** (projected served body with placeholders where A applied). Existing same-key chunk-transcript expansion semantics are untouched; this is a new resolver arm keyed by inherited-range membership, not a change to "raw" anywhere else.

## 2. Lifecycle: seal → reply → redeem
```
prepare(P, agent, F, source_segment) ──→ SEALED(receipt)      [pre-reply, durable]   | REFUSED
                                          │
                        gateway replies placeholder (only after SEALED)
                                          │
redeem(edge, receipt_id)  ──────────────→ REDEEMED(successor) [atomic bind + mint]
```
- **Seal point (fixes gap 2):** `prepare` ends with ONE store transaction (no awaits inside) that commits atomically: archive rows, receipt row `SEALED`, P's **admission fence** (`P.sealed_for = receipt_id`), and P's **epoch fence** (revert_epoch bump). From this instant: no P pass may mutate provider-visible bytes (any pass on P replays V for T and refuses new reductions); any P publish assembled before the fence is rejected by the existing guard; new pending drops for T are **not** appended to P's queue — they are recorded on the receipt as `post_seal_drops` and merged at redeem (§6). All await-bearing work (archive projection, optional in-flight grace) happens BEFORE this transaction; if it exceeds D the receipt is `REFUSED` and nothing is fenced.
- **Recovery never needs the harness's discarded source** (gap 2): after SEALED, everything the successor needs is in the archive. A crash after the reply and before redeem leaves a SEALED receipt; the next descend/redeem with the same edge material finds it by (P, agent, F) and redeems it. There is no `stale-at-descend → re-prepare` path after seal; revalidation at redeem is only `receipt.state == SEALED ∧ fence intact`, both under MC's control.
- **Redeem (fixes gap 3 second half):** one store transaction: bind edge (`receipt.edge`, once), mint successor row, copy compartments/transcripts/tags/archive, copy carried pending drops, set successor frontiers (§5), mark receipt `REDEEMED`. Either all rows exist or none. `REDEEMED` with a missing successor row is **corruption**: refuse loudly (`lineage_corrupt{receipt}`), never retry-mint, surface in health.
- **Cancellation paths without a gateway report (fixes gap 2 last clause):** MC cannot know whether the placeholder was delivered. Therefore a SEALED receipt is treated as *possibly delivered* until discharged. Discharge is positive only (§7). The gateway MAY send `cancel(receipt)` as a hint (`cancelled_before_delivery` — allowed only if the gateway asserts it never replied); MC records it, keeps the archive, and the fences on P release (P resumes mutable service) only via `release(receipt)` which the gateway sends with the same assertion; absent that, P stays fenced (safe: P is the lineage that was about to be abandoned; a fenced P still serves V byte-identically).

## 3. Admission barrier (fixes O4)
- Gateway owns ingress admission: before calling `prepare`, it installs a per-(session, agent) **ingress fence** with a boot/sequence watermark: requests with sequence > watermark are queued (bounded by D) or refused 503; requests ≤ watermark that are still collecting/decoding/streaming are **drained or aborted** by the gateway before `prepare` is sent. `prepare` carries the watermark; the receipt echoes it.
- MC fences its own side at seal (§2): publisher (no new fire on P), store (P immutable for T). MC never holds a SQLite transaction across a wait; the seal transaction is the only write in the critical section and it is synchronous.
- Existing `LineageCohort` in-memory tracking is not the fence (it is post-resolution); the durable watermark is.

## 4. Authoritative projection for every manifest block (fixes gap 4)
- `project(source, A)` is a pure function run at prepare over the supplied source segment: for each source block, if A has a unit for its identity → render per the unit (placeholder/skeleton/strip/caveman); else → render the block as the ordinary serializer would on a defer pass (no reductions, no summary-turn processing, no new minting of *decisions*).
- **Identity for never-yet-served blocks:** tag identities are minted deterministically from the source block's (mid, index) under the seal, using the same minting rule the tagger uses on a normal pass but with `decisions = none` (mint identity only; never mint reductions, strips, or frozen units at seal). These identities are written into A so redeem and later successor passes agree.
- Consequence: the archive is complete even if MC never served the newest blocks (results that arrived with the compaction request), and equal to what a defer pass would have served for already-served blocks (I2 pins both arms).

## 5. Representation on the successor (fixes gap 1)
- The successor **serves the carried V-tail on every forwarded pass** after m0/m1 and before the continuation anchor, until a **validated fold** covers it (ordinary successor historian, reading inherited ordinals from the archive) or a **legitimate reduction** retires individual blocks (ordinary successor reduction lanes operating on the inherited tag identities — the archive hash identifies the snapshot; it is not a permission to ignore later reduction state). No "admissible once" clause. Pins: first pass, second pass, post-restart pass all serve identical carried bytes absent new decisions; a fold on the successor retires the carried range and the next pass is a normal SOFT bust.
- Frontiers are three separate fields on the successor: `folded_frontier` (last real compartment end, e.g. 1798), `source_frontier` (end of the carried archive, e.g. 1939), `lineage_anchor` (continuation base, e.g. 1939/1940). The empty boundary row advances **only** `lineage_anchor`. The historian's eligible head = `folded_frontier + 1`, so inherited R is eligible immediately (I7).
- Receipt-declared carried blocks are validated by Thalamus by digest on every pass they appear (§8 manifest refs), not once.

## 6. Pending drops and reductions across the transition
- At seal: pending drops targeting T are frozen into A (`pending` with command provenance). Drops arriving after seal are recorded on the receipt (`post_seal_drops`) — never lost, never applied on fenced P.
- At redeem: pending + post_seal drops targeting carried blocks are copied to the successor queue (still queued-until-aged there, protection honored); drops whose targets were folded on P before the fence get ledger disposition `covered`. Ledger dispositions `partial`/`covered` require the banked store migration (#2732) — a hard dependency, sequenced first.
- Dropped bodies never resurrect: V already carries placeholders; the archive stores no dropped source bodies.

## 7. Retention and discharge (fixes gap 3)
- **No time-based reclamation of a SEALED archive, ever.** States and discharge:
  - `REFUSED`: no archive committed (refusal precedes the seal transaction) — nothing to retain.
  - `SEALED`: retained until positive discharge: `REDEEMED` (lives with the successor lineage; swept only by explicit lineage deletion) or `RELEASED` (gateway asserted never-delivered; archive kept 24 h *after release* for forensics, then swept — this is the only clock, and it starts at a positive event).
  - `cancel` hint without `release`: archive retained; P fenced; surfaced in `session.status` as `sealed_unredeemed{age}` so an operator can see a stuck transition; no auto-sweep.
- Explicit lineage deletion (`session.delete`) removes receipts/archives for that lineage atomically with its other rows.

## 8. Receipt schema (O3) — wire, versioned
```
receipt/v1 {
  receipt_id, attempt_id, schema_version: 1,
  predecessor_key P, session_id, agent,
  F: { digest, normalization_version, excluded_additions: [provenance tags] },
  sealed_state: { P_state_version, projection_key, applied_state_hash, epoch_before, epoch_after },
  ingress: { watermark, sequence_seen },
  frontiers: { folded: 1798, source: 1939, anchor: 1939 },
  representation: "carry" | "refused", refusal_reason?,
  manifest: ordered MESSAGE groups → BLOCKS: { ordinal, mid, index, role, kind,
             source: { len, sha256 }, served: { len, sha256 }, applied_unit?: key },
  refs: { archive_id, manifest_digest, carry_digest },      // per-pass validation by ref
  pending_drops: [{ command_id, target, state }], post_seal_drops: [],
  state: SEALED | REDEEMED | RELEASED | REFUSED, edge?: id, successor_key?: key
}
```
Per-pass validation uses `refs.carry_digest` + block digests; the full manifest travels once. Codec and anchor checks to be validated against current SubcTransform anchor logic (which assumes submitted-native anchors today) — a wire change on both sides, versioned.

## 9. Budget (fixes the 25% cap)
- Two separate caps, both stated in the refusal reason:
  - **Token headroom:** carry fits iff `estimate(system + tools + m0 + m1 + carry + anchor_reserve) ≤ usable_soft − output_reserve`, computed by the real estimator on the projected bytes at seal (same estimator the scheduler uses); no fixed percentage.
  - **Resource cap:** archive/carry encoded bytes ≤ a configured byte ceiling (default sized from the current 64 MiB frame body / page limits, stated in config, not policy) — protects the store and the wire, not the model.
- A carry that only fits by ignoring overhead is refused; refusal is before the seal transaction. No floor-policy changes.

## 10. Deadline D
Provisional 120 s total (ingress drain + projection + archive + optional ≤20 s in-flight grace + seal). Frozen only after Thalamus's hermetic CURRENT-CC compaction 503/timeout probe (isolated fake provider). Never scaled by fill.

## 11. Invariants (executable; replaces v2 §9)
- I1 No placeholder without SEALED (gateway pin); redeem without SEALED refuses (MC pin).
- I2 For every manifest block: successor first pass serves either a compartment covering it (folded before seal) or the carried block with bytes == archive V; blocks never served on P render as a defer pass would; applied units render as placeholders; no source body of a dropped block appears.
- I3 `ctx_expand(ordinal ∈ T)` on the successor returns archive V; existing chunk-transcript semantics unchanged (pin both).
- I4 Epoch guard unchanged; a P publish assembled pre-seal landing post-seal is rejected.
- I5 Idempotency: same (P, agent, F) → same receipt; retry-marker deltas do not mint; F change before seal → new attempt; after seal → the SEALED receipt wins (redeem finds it).
- I6 Seal atomicity: crash at any point before the seal txn leaves no fence and no receipt; after it leaves SEALED+archive+fences; redeem atomicity likewise; REDEEMED-without-successor is loud corruption.
- I7 Frontier split: successor historian eligible head = folded_frontier+1; specimen shape yields eligible_tokens > 0 on the first successor trigger.
- I8 Carry persistence: passes 1, 2 and post-restart serve identical carried bytes absent new decisions; a successor fold retires the range; a successor reduction retires a block.
- I9 Barrier: a request above the watermark during prepare is queued ≤ D or refused 503; never replayed, never dropped.
- I10 Retention: SEALED archives are never time-swept; RELEASED starts the only clock; `sealed_unredeemed` is visible in status.
- I11 Budget: refusal reason names which cap failed with the numbers.
- I12 The gap-accepting test is rewritten to a nonempty unfolded tail with the fixed placeholder asserting I2/I3/I7/I8.

## 12. Parity / dependencies
OpenCode/Pi: no D5. Depends on #2732 (ledger dispositions migration) and a versioned wire change on both seats. Athena gate after Thalamus agrees v3 and the receipt schema, with D's calibration evidence attached.

## 13. Normative amendments (v3.1, from Thalamus pm_aa06fec7 — folded, not editorial)

A) **SEALED P admission (replaces the "replay V" wording in §2).** After seal, P serves NO normal requests: managed turns arriving on P are queued behind the admission barrier (bounded by D) or refused 503 until REDEEMED or RELEASED. Appended source is never ignored and never replayed. The gateway's existing ban on transformed-body replay stands. I5 corrected: a request whose F differs from the SEALED receipt does NOT silently lose to the sealed one — redeem requires F match; mismatched material is refused (`seal_material_mismatch`) without deleting the archive and without discarding the new input (it stays queued/refused at admission, never dropped).

B) **Gateway durable attempt record.** Before the first reply byte, the gateway durably records `{attempt_id, P, agent, F, receipt_id, MAY_HAVE_REPLIED: false}` and flips `MAY_HAVE_REPLIED := true` (monotonic, durable) before writing the reply. Recovery keys on this record, never on finding F in a successor. RELEASE requires the gateway's proof of never-replied across ALL retries of that attempt (the record's bit false in every retry row for the attempt), not for one socket. After RELEASED, a new prepare with the SAME F is allowed and mints a NEW attempt/receipt (idempotency is scoped to non-RELEASED receipts); the archive is reused by content address.

C) **Two digest classes.** `manifest_digest` and `archive_id` are immutable and identify the sealed snapshot membership + source identities. `projection_digest{row_version}` is per-response and changes with every legitimate later reduction/tag/encoding change on the successor; per-pass validation compares the projection digest MC declares for THAT pass (with its row_version) — never the frozen carry bytes. §8 corrected: `refs.carry_digest` → `refs.projection_digest` (mutable, versioned); `archive_id` (immutable). After a newly applied drop, re-render and ctx_expand serve the reduced form; original bytes are never resurrected.

D) **Late-drop routing (any state).** A drop/command for a T target arriving at ANY time after seal is routed atomically by lineage lookup: P SEALED → recorded on the receipt; P REDEEMED → dedup by (command_id, target) and enqueue on the CURRENT successor if the target is carried there, else stamp ledger `covered` (folded) or `retired` (already reduced); P RELEASED → returned to P's own queue. No arrival is lost between copy and delivery; the route is a single store transaction keyed by the lineage table, retry-safe.

E) **Identity allocation for never-seen blocks.** MID allocation is the gateway's (its MID store); tag identity is MC's. `prepare` requires every supplied block to carry a gateway-allocated MID; MC never invents MIDs. Never-seen blocks: the gateway allocates MIDs idempotently for the attempt (same block bytes + attempt → same MID) BEFORE calling prepare, durably in its MID store keyed by attempt; MC then mints tag identities deterministically from (mid, index) under the seal — identity only, no decisions, no deferred commands bound, the summary instruction never processed as a turn. Crash recovery: gateway MID rows for an attempt persist; a repeated prepare with the same F reuses them; MC's seal is atomic so tag identities exist iff the receipt does.

F) **Geometry and freshness.** Token headroom uses the SAME estimator and reserves the scheduler uses (`usable_soft` is the scheduling denominator, not capacity): `estimate(system + tools + m0 + m1 + carry + anchor_reserve + output_reserve) ≤ usable_soft`, with an unknown estimate → refuse. Byte caps are two: the gateway ingress cap (64 MiB) and MC's subc frame/page capacity as configured for transform pages — the smaller governs; the final outbound gate still applies to every carry render. Freshness: the projection and any optional grace are computed BEFORE seal, so the seal transaction CAS-checks every input it depended on (A hash, coverage frontiers, P.state_version, pending-drop count); if an optional publisher committed meanwhile, the seal recomputes (bounded by D) or refuses — never seals a stale projection.

D calibration: Thalamus worker wi_1544da69 (current-CC native 503/delay, isolated fake provider) — results attach to the gate; D stays provisional until then. Receipt schema v1 updated per C (projection_digest{row_version}) and B (gateway attempt record referenced by attempt_id).

E-pin) **MID idempotency is positional.** The gateway's MID allocation key is `(attempt_id, predecessor P, ordered native MESSAGE position)` — a message-level identity; block index is separate and never part of the MID. Two byte-identical messages (or blocks) at different positions get distinct MIDs; a repeated prepare with the same F reuses the ordered allocation exactly. I2/I5 gain a duplicate-content regression: two identical messages in T must appear as two manifest entries with distinct MIDs on every prepare repetition and on the successor's served array.

## 14. D frozen and two post-refusal cases (v3.2, from Thalamus calibration pm_14a6176f)

**D = 120 s, server policy (frozen).** Evidence (CC 2.1.258, isolated fake provider, validated from raw request hashes and native boundaries, not worker labels): AUTO 503,503,200 recovers and the summary replaces all warm-up markers; AUTO persistent 11×503 retains all warm-ups, writes no summary/compact_boundary, and silently resumes the exact pending ordinary request with a 200; manual persistent 503 shows a compaction error and retains history; both manual and auto first responses tolerate a 120.003/120.004 s wait before a 503 and the next retry succeeds; 125 s success is tolerance evidence, not a client max. No 150 s extrapolation.

**Case A — ordinary P request immediately after a failed AUTO compaction (no user-visible error).** The gateway must classify by attempt state before forwarding: (i) prepare definitively REFUSED (receipt state REFUSED, or no attempt row exists) → transform the request normally on P (P was never fenced); (ii) attempt SEALED, or outcome UNKNOWN (e.g. the prepare call timed out) → do NOT forward raw and do NOT replay: query the attempt by (P, agent, F) (stable lookup handles a late commit), enforce the admission barrier (queue ≤ D or 503), preserving the appended input. Never lose or replay the appended request.

**Case B — client timeout at or near the seal commit is OUTCOME-UNKNOWN.** A gateway-side timeout cannot promise "nothing fenced"; only MC's REFUSED (durable) proves it. The gateway records the attempt as `outcome_unknown` and resolves it by the stable attempt lookup on its next touch of P; MC's seal transaction is atomic so the lookup returns exactly one of SEALED / REFUSED / absent (absent = never reached the seal, safe to treat as REFUSED after D has elapsed since the attempt's own start, recorded gateway-side).

Both cases and every §13 correction are Athena inputs. Evidence provenance: exact-version container, network=none; initial host runs were not OS-sandboxed and registered non-default URL handlers (documented by Thalamus); only four sanitized code/evidence files integrate; raw artifacts stay local to Thalamus.

## 15. Case B correction (v3.3, Thalamus pm_aa3598b5): absence is not proof

"absent after D → treat as REFUSED" is withdrawn. Elapsed wall time cannot rule out a delayed/queued prepare, a suspended process, or a seal transaction already in flight that commits after the caller's deadline; an expiry check before the transaction still races a long commit. The gateway retains UNKNOWN until MC provides an **authoritative absence-and-no-future-seal guarantee**:

- `attempt.resolve(P, agent, F, attempt_id)` — a durable MC operation that, in ONE store transaction, either returns the existing receipt state (SEALED / REFUSED) or, when no receipt row exists, writes a **tombstone** `REFUSED{reason: resolved_absent, attempt_id}` for that (P, agent, F, attempt_id). Any later seal transaction for the same key CAS-checks the tombstone and fails (`seal_after_tombstone`), so no seal can land after resolution. The tombstone is a receipt row in state REFUSED, retained under §7 rules (no archive to keep).
- The gateway's resolution of `outcome_unknown` is therefore: call `attempt.resolve`; SEALED → barrier/redeem path; REFUSED (real or tombstone) → P was never fenced or is now guaranteed never to be → transform normally. No read-then-admit; the write IS the read.
- MC-side: the seal transaction's CAS set gains the tombstone check; `preparing` rows are not a state the gateway can observe — only SEALED/REFUSED/absent exist to a resolver, and absent is converted to REFUSED-by-tombstone atomically.
- I13 (executable): a prepare whose seal txn is delayed past a concurrent `attempt.resolve` fails with `seal_after_tombstone` and leaves no fence; the resolve returns REFUSED; a resolve racing a seal that commits first returns SEALED (no tombstone written). Both orderings pinned with real two-connection interleaving.

§15 addenda (Thalamus pm_2e68e97a): `attempt.resolve` returns the existing state explicitly for ALL states — SEALED, REFUSED, REDEEMED (→ successor key returned; the gateway routes to it), RELEASED (→ P resumed; a new attempt with the same F is legitimate) — and writes a tombstone only when no row exists for (P, agent, F, attempt_id). Tombstone identity INCLUDES attempt_id, so a RELEASED or tombstoned attempt never blocks a later legitimate attempt with the same F. Review provenance: the running panel (ct_…c023bfb0b858) reviewed gate commit b1fd319a = v3.2; §15 (b60d6d88 = v3.3) is a subsequent normative amendment not covered by that panel and is folded into the final amendment review with the I13 interleavings before any spec mint.

## 16. Amendments from the salvaged round-0 panel reads (v3.4; ct_…c023bfb0b858 round 0, astra + grok — the panel saw the design truncated at line 109, so these are against v3.2 §1–§12 and are folded here as normative)

16.1 **Nested descent while carry is outstanding (grok #5 — a loss sequence).** A second native compaction on the successor S while its carried V-tail is still unfolded: the carry is MC-injected and NOT in Claude Code's native transcript, so the gateway's source segment for prepare(S, …) will not contain it and F' cannot cover it. Rule: prepare on any key whose receipt lineage has an outstanding carry (`source_frontier > folded_frontier`) MUST include the outstanding carried range as part of T for the new attempt, sourced from S's own archive (not from the gateway segment); the new archive is the union {inherited carry, S's uncovered native tail}; the manifest records provenance per block (`inherited_from: receipt_id` vs `native`). Descent chains are bounded by the existing merge-collapse cap; a chain carries at most the union of its unfolded tails, and the budget (§9) is evaluated over that union. I14: seed P→S with carry outstanding, descend S→S2 with the fixed placeholder, assert every P-tail manifest block is served on S2's first pass (content probe) and expandable.

16.2 **Per-block frozen digests for unreduced carry (grok #4 / §13C tightening).** §13C's mutable `projection_digest{row_version}` governs blocks whose applied state changed after seal. For a carried block with NO new decision since seal, the per-pass validation compares the block's served bytes to the FROZEN manifest `served.sha256` — the projection for an unreduced block is required to be a pure function of (source, A) with no clock/host input, and this is pinned (I8 tightened): defer, defer, restart serve byte-identical unreduced carry; a block reduced on S after seal validates by its new unit's placeholder bytes, declared in that pass's projection_digest with the unit key. A checker never compares a reduced block against the frozen archive bytes (no false refusal), and never accepts an unreduced block whose bytes differ from the frozen digest (no admitted mutation).

16.3 **Frontier split must rebind every coverage consumer (grok #7, astra #4).** Enumerated sites that today derive coverage from the terminal compartment / last-compartment-end and must be rebound to `folded_frontier` (with `lineage_anchor` used only for continuation/ordinal rebasing): `resolve_coverage` coverage_end_ordinal + boundary identity; `coverage_ordinal_from_compartments`; m0 compose boundary/coverage; m1 `new_coverage`; historian trigger `max_compartment_end_ordinal`; `assemble_historian_firing` chunk_start; ctx_expand `last_compacted_ordinal` and range-mode transcript bounds; `first_uncovered_live_block` / `validate_live_boundary_ordinal`; `descend_lineage` `coverage_ordinal = placeholder`; compaction-marker scheduling to the host (`boundary_id` in committed execute responses); the gap-accepting fake_compaction test. Each is a named executable pin in the spec (I15: a table-driven test that seeds the specimen shape and asserts each consumer reports folded_frontier=1798, not 1940). The empty lineage_boundary row is retained ONLY as an anchor row flagged `episode_type='lineage_boundary'` and is excluded from coverage derivation everywhere (today `decay_render` already excludes empty-payload rows from rendering; coverage derivation must do the same, gated to lineage_boundary rows so OpenCode/Pi sparse-ordinal behavior is untouched — 16.6).

16.4 **Budget inputs are on prepare (grok #9).** `prepare` carries a `budget` struct from the gateway: `{usable_soft, output_reserve, anchor_reserve, system_tokens, tools_tokens}` for the session's current model geometry as the gateway resolves it (the same values Thalamus already sends on TransformInput), plus MC's own m0/m1 estimates. With those, the refusal is decidable at prepare on module-held inputs; an absent field → refuse `budget_unknown` (no floor-policy fallback).

16.5 **REFUSED never falls into passthrough (grok #8).** A REFUSED attempt returns to the gateway as a typed refusal; the gateway replies 503 to the compaction request and MUST NOT route the compaction request or the following ordinary request through `lineage_protocol_passthrough` or any raw-forward arm; the ordinary request is transformed normally on the unfenced P (§14 Case A). Pinned: with prepare REFUSED, the next request's served array equals a normal P transform (content probe), and the raw over-limit array is never forwarded (size assertion).

16.6 **Receipts/archives ownership and deletion (grok #10, astra #6).** Receipts and archives carry BOTH `predecessor_key` and `successor_key` (nullable until redeem) and a `lineage_id`; `session.delete(P)` while a receipt is SEALED-unredeemed refuses (`sealed_unredeemed`) unless `force`, and after REDEEMED deletes only P-owned rows — the archive's ownership transfers to S at redeem (a row-level `owner_key` updated in the redeem txn), so deleting P never removes S's sole recovery source; deleting S removes S-owned archives; `last_activity` GC skips any key with a SEALED receipt; the RELEASED 24 h sweep is by receipt, and an archive blob is deleted only when no receipt in any state references its `archive_id` (content-addressed reuse across attempts is reference-counted, never time-swept). Both receipts tables are session-scoped tables added to the deletion/orphan-sweep list. Concurrent prepares for the same (P, agent, F): the seal CAS on P.state_version serializes them; the second sees the SEALED row and returns it (idempotent), never a second archive.

16.7 **Shared-path gating (grok #11).** Every change in 16.3 is gated on the presence of a `lineage_boundary` row / a receipt for the key; keys without D5 state take the existing code path unchanged, pinned by the differential goldens for OpencodeAiSdk and Pi profiles.

Provenance: 16.1–16.7 derive from the salvaged round-0 claims (file: mc-d5-gate/.cortexkit/alfonso/task-outputs/consult-ct_…c023bfb0b858.round0-verdicts.md); both seats confirmed the defect and the D=120 s freeze and read §13A–D as closing the earlier replay/immutable-digest/socket-release counterexamples. Two independent mason seats (wi_1c9e838b, wi_7ca5e6e8) are reviewing 87ec0ee9 (v3.3) with the full text; their findings merge with §16 into the final amendment review before any spec mint.

## 17. Corrections to §16 (v3.5, Thalamus pm_1142173a — source-checked)

17.1 **§16.4 is replaced.** TransformInput today carries the raw body + `geometry{usable_soft, usable_hard, derivation}` only (transform.rs:67–106; wire TransformRequest subc_transform.rs:139–193; WireUsage carries current_total_input_tokens/context_limit_tokens). No output_reserve / anchor_reserve / system_tokens / tools_tokens exist on the wire, and the gateway's usage meter does not observe split system/tool counts; `max_tokens` on the SUMMARY request is not the successor's output reserve; the summary turn's tools/system may differ from a normal turn. New **budget-evidence contract** on `prepare`: (a) `geometry` as today; (b) `envelope`: the identity (request id + sha256) of the most recent *normal-turn* outbound envelope on P that the gateway retained, plus its raw system and tools byte segments (not counts) — MC runs its own estimator over those bytes and over its m0/m1 and the projected carry, with estimator provenance recorded in the receipt; (c) `reserves`: `output_reserve` as MC's configured reserve for the model (MC-side, from window-geometry), `anchor_reserve` as a fixed MC constant for the continuation wrapper (measured from the placeholder + wrapper bytes, recorded); (d) **unknown ≠ zero**: any absent envelope segment or unresolvable geometry → refuse `budget_unknown`. No numeric split counts are demanded from the gateway; diagnostic captures are not authoritative inputs.

17.2 **§16.1 premise confirmed and sharpened.** The native source segment excludes MC-injected carry by construction (controls 13615/16/17: native 4/6/8 messages vs forwarded 6/8/10; m0/m1 absent from every native message); the gateway's view of MC's outbound projection is a different surface from the next native request and is not source custody. Nested prepare rule as stated in 16.1, with three additions: the union {inherited archive, genuinely new native source} is deduplicated by **provenance/identity** (receipt_id + block identity), never by content hash; CURRENT reduction state A on S is applied when projecting inherited blocks (reusing the original archive V must never resurrect a drop applied on S after the first descent — the inherited block's projection is recomputed under S's A, and the frozen per-block digest for it is re-minted under the new seal); and the chain preserves provenance across archive collapse (the new manifest records the originating receipt for every inherited block).

17.3 Review provenance: the panel review is NOT complete — round-0 claims were salvaged, rounds ≥1 never produced; the two mason seats and a refired panel on the final text are the gate. Thalamus's bounded coverage-consumer map (wi_sk_bg_4510ddad) feeds 16.3's site list; reviewers get §17 before any gating or coding.

## 18. Amendments from the two independent seats (v3.6; seat A = wi_1c9e838b, seat B = wi_7ca5e6e8, both AMEND on v3.5; reports under .cortexkit/alfonso/reviews/d5-uncovered-tail-2026-09-11/design-gate-seat-{A,B}.md). Converging findings are merged; each carries the constructed sequence in the seat report.

18.1 **Resolve reports the current P fence, not one attempt's fate (B1, A1 first half).** `attempt.resolve` returns `{attempt_outcome, p_fence: {state: none|SEALED{receipt_id}|REDEEMED{successor_key}, fence_generation}}` in one transaction; the gateway admits an ordinary P request only on `p_fence.state == none`, never on `attempt_outcome == REFUSED` alone (a REFUSED/tombstoned attempt proves that attempt cannot seal, not that P is unfenced — another attempt may hold the fence). Coalesced attempts: the seal transaction records every concurrent attempt_id that observed the SEALED row as a durable alias on the receipt, so resolve(any alias) returns SEALED. Normal P admission CAS-checks `fence_generation` independently of any attempt state. Gateway side: the durable attempt identity + ingress ownership record is written BEFORE dispatching `prepare` (not merely before the first reply byte — A1: a crash between prepare and the attempt record otherwise classifies a SEALED P as unfenced); after restart the gateway queries P's active receipt before admitting any ordinary request.

18.2 **Release is a terminal send-revocation protocol (A1 second half, B2).** Gateway: an atomic durable transition `RELEASE_INTENT → NEVER_SEND` that serializes with every reply permission and forbids registering new retries or flipping any bit afterwards; a retry worker holding a cached SEALED response must re-check `NEVER_SEND` under the same lock before emitting the placeholder. MC: `release` is a CAS `SEALED → RELEASED` (idempotent, refuses on any other state) that serializes with `redeem` (both CAS the same receipt row; exactly one wins). **Transfer on release:** the release transaction merges every receipt-held command (`pending` frozen into A at seal and `post_seal_drops`) back onto P's queue with provenance and dedup by (command_id, target), preserving ledger rows, and clears the admission fence in the SAME transaction — no parked drop is ever stranded on a swept receipt. Pins: C-before-release, C-after-release, concurrent release/routing, release-vs-reply, release-vs-redeem, crash-before-gateway-attempt-record.

18.3 **Two anchor identities, and publication across the retained boundary row (A3, B3).** The core and wire distinguish `coverage_identity` (the last real compartment's end: `mid1798#idx`, used by m0/m1 composition, historian eligibility, ctx_expand range, coverage derivation) from `continuation_identity` (the native anchor `mid1940`, used by ordinal rebasing, the host compaction marker, reconcile/trim, and the live-boundary availability guards at transform.rs:4812–4821/4912–4917/7866–7904). Rule: the live-boundary guards validate the `continuation_identity` against the native request (present by construction) and the `coverage_identity` against the RECEIPT (a receipt-backed presence proof: the block is in the carried manifest or in a real compartment) — never a global bypass of mint/revert guards, and never a numeric substitution. Historian publication on S may cross the retained `[1940,1940]` anchor row: the store's overlap check (mc-store lib.rs:16663–16690) exempts rows with `episode_type='lineage_boundary'` from overlap (they are non-covering anchors kept in a separate role), a crossing fold either absorbs the anchor row (anchor metadata preserved on the new compartment) or the anchor is moved to the fold's end — one rule, pinned by: partial inherited fold (1799–1905), fold crossing the anchor (1799–1950), repeated/nested folds, and assembly-snapshot/generation ordering vs publication sequence.

18.4 **Optional folded frontier and the zero-compartment case (A5).** `folded_frontier` is `Option<ordinal>`; with no real compartment on S, the historian's eligible head is `first_inherited_ordinal` (the carry's first manifest ordinal), not `folded_frontier + 1`; lib.rs:5198–5226's `continued_ordinal_offset_missing` arm must not fire when a carry exists; ordinal 0 is a real CC ordinal (lib.rs:11902–11905) — no unsigned arithmetic assumes ≥1. Pins: no-fold-yet with carry; a real fold ending at ordinal 0; the specimen 1798/1940 shape.

18.5 **Negative-state retention and generation-fenced deletion (A4, B4).** Tombstones are never reclaimed while the attempt could still seal: every prepare/resolve/seal carries the session's `incarnation` (a monotonic generation bumped on session.delete/recreate); a delayed prepare with a stale incarnation refuses at the seal CAS regardless of tombstone presence, which makes tombstone rows reclaimable after the incarnation advances. Admission quota: a per-session cap on outstanding non-terminal attempts (refuse `attempt_quota` before allocation) and a bound on retained negative rows per session (oldest-first retirement only when their incarnation is stale). Deletion scope: receipts/archives are session-scoped rows keyed by `owner_key`; `deleteSessionScopedRows` (mc-store lib.rs:7444–7485) deletes by `session_id` today — the D5 tables are deleted by `owner_key`, which the redeem transaction transfers to S, so deleting P after redeem cannot remove S's protecting reference; blobs are refcounted by `archive_id` across receipts in every state including tombstone-free negative rows; nested descents transfer ownership hop by hop; a stale request after delete/recreate is refused by incarnation. Forced deletion is the one intentional destructive exception and is logged as such.

18.6 **Count the output carve exactly once (B5).** `usable_soft` is already `window − output_reserve` (window-geometry.ts:514–620; Rust geometry is host-resolved). The budget test is `estimate(system + tools + m0 + m1 + carry + anchor_reserve) ≤ usable_soft` — output_reserve is NOT added again. If an additional safety margin is wanted it is a named separate constant, not the scheduler's reserve. Pins: shared-upfront, shared-truncating and separate geometries, explicit output overrides; the 200k/20k configured case (170k projected fits, must not refuse).

18.7 Provenance: both seats read v3.5 at gate 218c6345 with the Rust sources at 711b097e; neither saw the other's report; both independently converged on 18.1–18.3 and 18.5. Panel state: Athena round 0 salvaged (§16), rounds ≥1 lost to a harvest-validation defect ALF is fixing; a refire on the FINAL text (this section included) is the remaining review before the spec mint, along with Thalamus's consumer map feeding 16.3/18.3.

## 19. Budget accounting made explicit; admission semantics (v3.7, Thalamus pm_548e4661)

19.1 **Reserve accounting, stated per source.** The CC gateway's wire geometry (thalamus proxy.rs:185–205) is NOT net of an output reserve: for context-1m models `usable_hard == usable_soft` with no subtraction; for others `usable_soft` is a configured scheduling value (default 167k) and `usable_hard` = 200k, overrides may exceed hard; TransformInput carries no output_reserve. MC's OpenCode/Pi geometry (window-geometry.ts:514–620) DOES carve `output_reserve` into `usable_soft`. So "the reserve was removed once" is true on the TS legs and "zero times" on the CC wire. The budget therefore uses two named variables and states the conversion: `fit_soft = usable_soft_wire` when the wire declares its geometry already net of reserve (a `derivation` flag the geometry struct carries — MC legs set it; CC does not) and `fit_soft = usable_soft_wire − output_reserve_mc` otherwise, where `output_reserve_mc` is MC's model-resolved reserve; **and independently** the hard test `estimate(...) + output_reserve_mc ≤ usable_hard_wire` — soft alone never certifies fit. Overrides above hard are clamped to hard for the fit test. Both variables, the flag and the clamp are recorded in the receipt's budget block. Pins: CC 167k/200k geometry with a carry that fits soft but not hard (refuse), the 1m case (hard == soft), the TS-leg case (reserve counted once), an override above hard (clamped).

19.2 **fence_generation is consumed at admission, not cached.** The gateway either holds exclusive ingress ownership for the (session, agent) across resolve → admit, or re-checks `fence_generation` in the actual admission step; a value read earlier is never treated as standing permission. Pin: resolve returns none, a concurrent prepare seals, the admission re-check refuses.

19.3 **Release safety recorded as target semantics.** 18.2's gateway clauses are safety semantics to implement, not a claim about today's gateway; NEVER_SEND spans all aliases/retries for a receipt; if MAY_HAVE_REPLIED is already true, release is never claimed safe; revocation precedes the remote release call; cached SEALED responses re-check permission immediately before acquiring the send right; MC restores frozen + post-seal commands to P atomically on release.

19.4 Remaining before spec: Thalamus's consumer map into 16.3/18.3; the estimator/retained-envelope lifecycle (how long the gateway retains the normal-turn envelope, what invalidates it, what MC does when it is absent — currently `budget_unknown` → refuse) as an explicit section; then the Athena refire on the final text. Status: AMEND folded, not SHIP-DESIGN.

§19 pins (Thalamus pm_dfd19def): (a) wording: the CC wire carries **no declared output-reserve subtraction** (not "no harness buffer"); (b) the net-of-reserve indication is a NEW versioned structured field on the geometry struct (`reserve_accounting: {once_carved | none_declared}`), never parsed from the human-readable `derivation` string; CC's value is a new declared semantic; (c) the fixture table: default 167k/200k proves once-only reserve accounting; the independent hard-rejection counterexample uses a soft OVERRIDE above hard (X ≤ soft but X + R > hard) — "fits soft not hard" at 167/200 with R ≥ 0 is impossible and is struck; over-hard candidates are REFUSED with the reported geometry left untouched (no silent clamp-and-rewrite; the clamp exists only inside the fit test); (d) before claiming carry liveness, size the preserved specimen (83 tags / ~24k stored tokens, plus system/tools/m0/m1/anchor) under the proposed reserve — a conservative soft policy refusing something that fits physical hard capacity is a deliberate safety/liveness choice to state, not a provider restriction; (e) the receipt's budget block names the reserve SOURCE and UNITS (`output_reserve_mc{model, tokens, source: window-geometry|config}`) and enforces unknown ≠ zero. Retained-envelope lifecycle section still owed (19.4).

## 20. Retained-envelope lifecycle (v3.8; closes 19.4)

The budget-evidence contract (§17.1) needs the most recent NORMAL-turn outbound envelope on P as the source of the system and tools byte segments. Lifecycle:
- **Retention (gateway):** the gateway retains, per (session, agent), exactly ONE envelope record: `{envelope_id, request_sha256, captured_at, model, system_bytes, tools_bytes, geometry, reserve_accounting}` from the most recent forwarded normal turn (not a compaction/summary request, not a refused/503 turn, not a retry). Overwritten on each newer normal turn; never partially updated (write-then-swap).
- **Invalidation:** a model change, a tools-set change (sha of the tools segment differs), or a system change (sha differs) makes the record simply the newer one — there is no "stale" state; the record is always the latest observed. It is absent only before the first normal turn of the session or after a gateway restart that did not persist it (persistence is required for the record to survive restart; if the gateway cannot persist envelopes, absence after restart is the honest outcome).
- **Use at prepare:** the gateway passes the record by value; MC records `envelope_id` + `request_sha256` in the receipt's budget block and runs its estimator over the byte segments. MC never caches the envelope beyond the receipt.
- **Absence:** no record → `budget_unknown` → REFUSE (503 to the compaction request; the harness's own compaction proceeds; no seal, no fence). This is the deliberate liveness cost of unknown ≠ zero; it is expected only on the first turn of a fresh session or immediately after a non-persisting gateway restart, both stated in the refusal reason.
- **Mismatch:** if the envelope's model differs from the compaction request's model, MC refuses `budget_model_mismatch` (the summary turn's model is not the successor's serving model; the estimator has no basis).
- **Pins:** first-turn compaction refuses budget_unknown; a compaction after a normal turn seals with the envelope id recorded; a tools-set change between the normal turn and the compaction uses the NEWER record (no stale refusal); a gateway restart without persistence refuses with the stated reason; model mismatch refuses.

Design status after §20: AMEND-folded through both seats and Thalamus's corrections; the FINAL review inputs are (1) Thalamus's consumer map (pending) merged into 16.3/18.3's table, (2) the Athena refire on the final text once ALF's harvest fix is live. Spec mint after that; no implementation before.

§19(d) sizing guard (Thalamus pm_b9f13187): the specimen's "83 tags / ~24k stored tokens" is the retained TAGGED-fragment mass only (the defect report limits that number explicitly); the carry estimate for sizing and for the budget test is the estimator over the FULL projected V-tail from the manifest — untagged tool inputs, reasoning blocks, system blocks and the actual projected reductions included — never the tag sum.

§20 gateway constraints (v3.9, Thalamus pm_9c63ecbb — normative, carried into the final gate):
1. **Retry-inclusive selection.** A successfully forwarded normal turn that was itself a harness retry IS the latest valid normal envelope; exclusion applies to rejected/unmanaged/summary attempts, not to retried requests (otherwise a 503 recovery with new tools/model leaves an older record indefinitely).
2. **Causal-order swap.** The record is replaced only when the incoming envelope's `(gateway boot id, ingress sequence)` is strictly greater than the stored record's, and the session/agent/lineage/incarnation match; an older overlapping request that completes last is discarded, not written. The update point is the moment the forwarded request is committed to send (not on completion). A "newer-unusable" tombstone: when a newer normal turn was forwarded but its envelope could not be persisted, the record is marked `newer_unusable{sequence}` so an older record is never presented as current — absence with a reason, not a stale record.
3. **Persistence required.** Restart survival is promised only when the gateway persists the record durably BEFORE the provider send (write-ahead: persist → send; a crash between the two leaves a record that describes a request that may not have been sent, which is still a valid envelope description); a non-persisting cache is a degraded mode that must be declared and yields `budget_unknown` after restart — it is not the design. Disk write failure → mark the slot unusable (tombstone as in 2). A restored older gateway DB is detected by the incarnation/boot-id in the record disagreeing with the live session incarnation → absent, never "fresh by presence".
4. **Envelope is evidence of THAT request only.** The record carries header-derived geometry, profile, guidance-surface and tool-surface identities (hashes) alongside the raw segments; at prepare MC validates envelope identity against the compaction request's declared identities (model equality alone is insufficient) and the receipt records age (sequence distance) and any identity mismatch as `budget_evidence: {fresh | aged{n} | mismatch{field}}` — mismatch on tools/system/profile refuses `budget_evidence_mismatch`; the final outgoing-request geometry check on the successor's first pass remains mandatory regardless (the estimate is admission evidence, not a guarantee); summary-request `max_tokens`/tools are never substitutes.
5. **REFUSE semantics restated.** "503, harness compaction proceeds" means the COMPACTION FAILED at the gateway: the tested AUTO path resumes its ordinary request with old history (no provider-summary fallback is performed by anyone); no destructive placeholder and no fence exist only on an authoritative REFUSED; UNKNOWN still resolves through the attempt machinery (§15/§18.1).
These are implementable constraints; no current gateway persistence API is claimed to exist.

§20 cautions (Thalamus pm_301224c3, final-gate inputs): (a) a restored/rolled-back gateway DB cannot be detected from ids stored only inside that DB; the restore fence needs an INDEPENDENT witness — the session incarnation held by MC (store.db) is that witness: at prepare, MC compares the envelope's recorded incarnation against its own; an envelope older than MC's incarnation for the session is absent-by-rollback. Ordinary restart changes boot id by design and must NOT invalidate an otherwise durable envelope — boot id is used only for causal ordering within a boot, incarnation for validity across restarts; restart survival is claimed for that bounded guarantee only. (b) Summary requests may legitimately differ from or omit fields of the normal envelope; identity validation is against the intended NORMAL projection identities the successor will serve under (model, tool-surface identity as MC resolves it for the session, guidance surface), never blind equality to the raw summary shape. Consumer map: seat A (§Q4) and seat B (§4) each produced a verified consumer inventory; §16.3/§18.3's table is built from their union with explicit gaps marked, and Thalamus's supplementary map (bg_4510ddad) is merged when delivered — it is not a blocker for the final gate.

§20 witness scope (Thalamus pm_44b35e6c, for the refire): the incarnation witness is BOUNDED — an envelope with an OLDER incarnation than MC's is rejected; an EQUAL incarnation does not detect a rollback to an older envelope from within the same incarnation, and a joint restore of MC's store rolls the witness back too. Those two cases are explicit limitations, not covered: freshness there depends on the recorded normal-projection evidence agreeing with the compaction request's declared identities (18.4-style validation), and where that cannot decide, the outcome is UNKNOWN/REFUSE, never a claim of universal rollback detection. Counterexample carried into the refire questions.

## 21. Amendments from the refired panel (v3.10; ct_…6025b2734778 — one surviving seat, grok, full text read after a need_more_context round; verdict AMEND with five spec silences, no remaining loss/placeholder/raw-forward sequence)

21.1 **Different-F prepare while P is SEALED.** `prepare(P, agent, F2)` while P holds SEALED for F1 → `REFUSED{p_already_sealed, receipt_id: R1}` — never returns R1 as if it were F2's receipt (the gateway must not emit F2's placeholder against F1's archive), never deletes or alters R1. Pinned with F1 sealed, F2 in flight, both orderings of the seal.
21.2 **Redeem retry after REDEEMED.** `redeem(receipt_id)` on a receipt already REDEEMED returns the existing `successor_key` (idempotent success); a second successor is never minted. Pinned: redeem commits, gateway times out, retries → same key.
21.3 **Resolve at the negative-row cap must still fence.** When `attempt.resolve` must write a tombstone but the per-session negative-row bound is saturated with current-incarnation rows, it does NOT fail open: the session row carries a `resolve_generation` that resolve CAS-bumps in the same transaction, and every seal transaction CAS-checks `resolve_generation` unchanged since the attempt's prepare started; a bump makes any in-flight seal fail `seal_after_resolve` even with no tombstone row written. Tombstone rows become an optimization for per-attempt reporting; the fence is the generation. (Closes the seal-after-resolve window §15 was written for, independent of row caps.)
21.4 **Absorb, not move.** A successor fold that crosses the retained `lineage_boundary` anchor row ABSORBS it: the anchor metadata (`continuation_identity`, native anchor mid) is preserved on the new compartment and the anchor row is removed in the same publish transaction. `continuation_identity` never moves after redeem; only `coverage_identity` advances with folds. I15 pins this layout; the "move" alternative is struck.
21.5 **Gateway MID rows are bounded.** MID rows `(attempt_id, P, position)` are reclaimable once their attempt is terminal (REDEEMED/RELEASED/REFUSED-tombstone) AND the session incarnation has advanced or the receipt's retention has elapsed (§7 rules); non-terminal attempts are bounded by `attempt_quota` (§18.5). Stated as a gateway target semantic (no Thalamus source today).

Review ledger: original panel round 0 (§16), seat A + seat B (§18), Thalamus corrections (§17, §19, §20), refired panel single seat (§21). The synthesis itself flags single-seat breadth; amendments 21.3 and 21.5 are the two it asked to have re-read — 21.3 is now generation-based rather than row-based (a stronger construction than the one reviewed), 21.5 is gateway-side. Design complete pending Thalamus ack on 21.1/21.2/21.5; next step is the spec draft (work draft), whose own adversarial rounds are the further review.

§21 acks and one correction (Thalamus pm_9e73828e): 21.1 ACK (F2's input preserved; ordinary P never admitted from an attempt REFUSED alone); 21.2 ACK (no second lineage or MID/tag remint); 21.5 ACK narrowed — reclaim only attempt-ALLOCATION bookkeeping after proven terminality with all aliases non-replyable and no receipt/carry/archive reference; canonical MID high-water / issued identities / IDs carried across descendants are never reclaimed or reused; no TTL-only deletion of MAY/UNKNOWN/non-terminal attempts; the terminal replay answer (or an immutable reconstruction) is preserved for idempotency. **21.3 corrected:** a generation read at MC-arrival time does not close the race — an old prepare queued BEFORE resolution but arriving AFTER the increment reads the NEW generation and seals. Rule: the gateway acquires an **admission ticket** `{resolve_generation, P, agent, incarnation}` and writes it into its durable attempt record BEFORE dispatching prepare; MC checks the ticket's generation at admission AND at the seal CAS against the current session `resolve_generation`; a retry of the same attempt never refreshes its ticket (a genuinely new attempt acquires a new ticket). Pinned: old request delayed before handler arrival, resolve bumps at cap, old request arrives → refused, no fence; a new ticket + attempt then succeeds; counter monotonic across restore (incarnation fence) and SEALED/REDEEMED resolution explicit. Provenance: §21 is the survivor's AMEND folded, not independently cleared; it goes to the spec's adversarial rounds. D5 implementation/live hold remains.

§21/55 timing distinction (Thalamus pm_8c49a4ea, ruling): an unsupported or unmeasured profile, or any budget input missing AT PREPARE, is `budget_unknown` → REFUSE under the pre-seal contract; preserved-but-blocked governs only an unforeseen or mismatched ACTUAL successor capacity after a valid seal and never replaces the pre-seal unknown refusal. The policy_reserve calculation handoff names the exact mc-tokenizer function and version, model, profile and both fixture-manifest hashes, and labels every number as an estimate, not a provider measurement. No blind fixture expansion: the current set establishes the known shapes and variance; a specific missing profile is requested by name if the policy needs it.
